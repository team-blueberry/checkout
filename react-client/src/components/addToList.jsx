import React from 'react';

const AddToList = (props) => {
  return (
    <div>
    <hr />
    <button type="button" className="addToListButton">Add To List </button>
    </div>
  )
}

export default AddToList;
